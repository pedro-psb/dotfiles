# Tmux config

There is also a `plugins/` dir which is managed by TPM, but it
isn't really configuration and shouldn't be track by git.

## Notes

May be worth having a look this article about organizing tmux on XDG-compatible dirs.
<https://nickjanetakis.com/blog/put-all-of-your-tmux-configs-and-plugins-in-a-config-tmux-directory>
